import 'package:flutter/material.dart';

class DetailShipper extends StatelessWidget {
  const DetailShipper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
