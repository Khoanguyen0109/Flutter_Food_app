import 'package:flutter/material.dart';

class FindingShipper extends StatelessWidget {
  const FindingShipper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
